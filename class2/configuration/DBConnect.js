const mongoose=require("mongoose");
async function getDBConnection(){

    try{
          const isConnected=await company
    }
    catch(error){
    console.log(error,"error in DBConnection")
    }
}
module.exports=getDBConnection;